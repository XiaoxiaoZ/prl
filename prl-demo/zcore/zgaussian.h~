#ifndef ZCORE_GAUSSIAN_H
#define ZCORE_GAUSSIAN_H

#include <iostream>
#include <math.h>
#include "Eigen/Eigen" 

extern double gaussian(double x, double mu, double sigma);
extern double gaussianM(Eigen::VectorXd x, Eigen::VectorXd mu, Eigen::MatrixXd sigma);

#endif
